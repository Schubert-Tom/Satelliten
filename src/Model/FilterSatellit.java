package Model;

public class FilterSatellit extends Filterarg {
    String kriterium;
    public void filter(Satellit sat){
        // filtert übergeben daten nach kriterium
        // Streamof(sat).filter(nfilter-> nfilter.equals(this.kriterium)) // igw so ka
    }
}
